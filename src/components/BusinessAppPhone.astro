<div class="phone-scene" role="img" aria-label="Illustrative business web app inside a phone. Workday shows a new quote request assigned to Alex, a scheduled site visit assigned to Jamie and a completed follow-up. Sample tasks, not a live application.">
  <div class="phone" aria-hidden="true">
    <div class="device-top"><span>9:41</span><span class="speaker"></span><span>100%</span></div>
    <div class="app-heading"><span class="app-logo">W</span><span>Workday<small>Your business, organised</small></span></div>
    <div class="greeting"><h3>Keep work<br />moving.</h3><p>One place for the next step.</p></div>
    <div class="section-label"><strong>Today’s priorities</strong><span>3 tasks</span></div>
    <div class="task"><span class="status new">New request</span><h4>Prepare a quote</h4><p>Office maintenance enquiry</p><div class="task-meta"><span class="avatar">AL</span><span>Alex · Review details</span></div></div>
    <div class="task"><span class="status scheduled">Scheduled</span><h4>Site visit</h4><p>Confirm access with the customer</p><div class="task-meta"><span class="avatar">JM</span><span>Jamie · Today, 2:00 pm</span></div></div>
    <div class="completed"><svg viewBox="0 0 20 20"><path d="M4 10L8 14L16 5"/></svg><span>Customer follow-up complete</span></div>
    <div class="app-tabs"><span class="active">Requests</span><span>Tasks</span><span>Team</span></div>
    <div class="home-bar"></div>
  </div>
  <p class="sample-note">Example business web app · Sample tasks</p>
</div>
<style>
  .phone-scene { margin-block:28px; }
  .phone { width:min(100%, 320px); box-sizing:border-box; margin-inline:auto; padding:16px; border:2px solid #82a99b; border-radius:34px; background:#091b17; color:#eef7f2; }
  .device-top { display:flex; align-items:center; justify-content:space-between; font-size:11px; padding:0 6px 22px; }
  .speaker { width:60px; height:7px; border-radius:8px; background:#496c60; }
  .app-heading { display:flex; align-items:center; gap:10px; font-size:17px; font-weight:650; }
  .app-logo { display:grid; place-items:center; width:34px; height:34px; background:var(--mint); color:#08231c; border-radius:8px; }
  small { display:block; font-size:10px; color:#b8cec5; font-weight:400; margin-top:3px; }
  .greeting { padding-block:25px 24px; }
  h3 { font-size:32px; line-height:1.05; letter-spacing:-.025em; margin:0 0 9px; }
  p { font-size:12px; line-height:1.5; margin:0; color:#bfd5ca; }
  .section-label { display:flex; justify-content:space-between; gap:8px; font-size:12px; padding-bottom:12px; }
  .section-label > span { color:#b8cec5; }
  .task { padding:14px 0; border-top:1px solid #38594d; }
  .status { display:inline-block; font-size:10px; padding:4px 7px; border-radius:4px; }
  .new { background:#16483b; color:#8ff2d2; }
  .scheduled { background:#37321e; color:#f2d792; }
  h4 { font-size:16px; margin:9px 0 5px; line-height:1.3; }
  .task-meta { display:flex; align-items:center; gap:7px; margin-top:12px; font-size:10px; color:#c2d8cd; }
  .avatar { display:grid; place-items:center; width:23px; height:23px; border-radius:50%; background:#24483b; font-size:9px; }
  .completed { border-top:1px solid #38594d; padding-block:16px; display:flex; align-items:center; gap:7px; font-size:10px; color:#9ce8ca; }
  .completed svg { width:16px; flex-shrink:0; fill:none; stroke:currentColor; stroke-width:2; }
  .app-tabs { display:flex; justify-content:space-around; border-top:1px solid #38594d; padding-top:14px; color:#b8cec5; font-size:11px; }
  .active { color:var(--mint); font-weight:650; }
  .home-bar { width:80px; height:4px; border-radius:4px; background:#82a99b; margin:18px auto 0; }
  .sample-note { text-align:center; margin-top:16px; font-size:12px; }
</style>
